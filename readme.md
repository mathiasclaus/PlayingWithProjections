# Playing with Projections in Java
## Getting started: cli
`./gradlew execute  --args='[path to json file]` e.g. `./gradlew execute  --args='../data/0.json'`

## Todo
- add a docker way to play with Java without installing stuff
